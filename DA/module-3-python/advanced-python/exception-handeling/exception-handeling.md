# exception-handling  ?

  1. exception handling is used to a block execution for find exception or error in python
  2. exception handling is executed with try and except block
  3. exception handling find exception or error 
  4. exception generate finally block that can be iterate multiple checks inside of finally block or its check let try and except is finished or not

  ```
  syntax

  try:
    statements
  except:
    statements
  finally:
    statement

  ```
